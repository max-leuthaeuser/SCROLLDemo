akka {
  loglevel = DEBUG
  log-dead-letters = 0
  log-dead-letters-during-shutdown = off
}

spray.can {
  server {
    request-timeout = 1s
    ip = "localhost"
    port = 8080
    request-timeout = 60 s
    idle-timeout = 120 s
    parsing {
      max-uri-length = 16k
    }
  }
}
